name: "Node.js"
on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]


jobs:
  build:
    name: "print"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: print stuff
      - run: git fetch --prune --unshallow
      - run: env
      - run: ifconfig
